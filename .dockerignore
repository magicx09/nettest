# ---------------------------------------------------------------------------
# Docker 构建上下文排除清单
#
# 为什么必须有这个文件：
#   docker build 会先把整个上下文发给 daemon，且 Dockerfile 里任何 COPY
#   都可能把这些东西永久烧进镜像层。其中两类绝对不能进镜像：
#     * config/audit.env —— 里面有你的订阅 token 和 API Key
#     * out/             —— 里面有全部节点地址、出口 IP 和历次报告
# 少了这个文件，`docker build` 时不报错，但推送镜像就等于把订阅链接公开了。
# ---------------------------------------------------------------------------
out/
dist/
.git/
.gitignore
.github/
*.log
*.tmp
.DS_Store
__pycache__/
*.pyc

# 订阅与密钥：只允许 audit.env.example（空的模板）进镜像
config/audit.env
